Gnome
=====

## Gestionar Bookmarks de nautilus ##

editar `~/.config/user-dirs.dirs` y comentar la línea que se desea suprimir

     XDG_VIDEOS_DIR="$HOME/Videos"

a

     #XDG_VIDEOS_DIR="$HOME/Videos"

crear o editar `~/.config/user-dirs.conf` y agregar

     enabled=false
  
